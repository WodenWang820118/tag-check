---
name: nx-run-tasks
description: '⚠️ MERGED into nx-workspace. Load `nx-workspace` instead for both exploration and task execution.'
---

# ⚠️ This skill has been merged

`nx-run-tasks` is now part of `nx-workspace`. Load the `nx-workspace` skill
for all Nx exploration and task execution needs.
