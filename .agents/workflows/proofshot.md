# Proofshot Workflow

⚠️ This workflow has been merged into the `proofshot` skill. Load
`.agents/skills/proofshot/SKILL.md` for the canonical proofshot workflow and
guardrails.

Use `.agents/references/proofshot-targets.md` for repo-specific browser target
routing.
