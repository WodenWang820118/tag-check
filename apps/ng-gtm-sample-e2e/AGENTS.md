# AGENTS

This directory contains the `ng-gtm-sample-e2e` Nx project.

Use the repository root [AGENTS.md](../../AGENTS.md) as the canonical source
for workflow, review gates, reviewer routing, and skill selection.

Local purpose: Browser end-to-end coverage for the GTM sample site.

Prefer local `project.json` targets for task discovery and keep this file as a
thin bridge only.
