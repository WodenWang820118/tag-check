{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allows the main window to manage the local backend sidecar.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:allow-kill", "shell:allow-spawn"]
}
