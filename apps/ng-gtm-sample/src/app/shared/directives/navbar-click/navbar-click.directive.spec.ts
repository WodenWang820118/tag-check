import { NavbarClickDirective } from './navbar-click.directive';

describe('NavbarClickDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarClickDirective();
    expect(directive).toBeTruthy();
  });
});
